import { Component } from '@agros/app';

@Component({
    file: '<%= file %>',
    <% if (typeof lazy === "boolean") { %>
    lazy: <%= lazy %>,
    <% } else {} %>
})
export class <%= name %>Component {}
