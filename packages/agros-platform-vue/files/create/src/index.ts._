import { createWebHashHistory } from '@agros/platform-vue/lib/vue-router';
import { AppModule } from '@/app.module';

export default {
    module: AppModule,
    container: document.getElementById('root'),
    RouterComponent: createWebHashHistory(),
};
